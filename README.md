# strategy_validator

## Setup

Create `.env`:

```env
GEMINI_API_KEY="YOUR_API_KEY"
```

## Run

```bash
uv sync
uv run validate strategies/surge-10.md
```

You can also run another strategy file in `strategies/` by replacing `surge-10.md`.
